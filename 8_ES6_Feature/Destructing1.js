// assigning object attributes to variables
const book= {
    title: 'Atomic Habit',
    writer: 'anish'   
}

// destructuring assignment
let {title,writer } = book;

console.log(title); // Atomic habit
console.log(writer); // anish